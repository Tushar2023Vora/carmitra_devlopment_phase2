import React, { ImgHTMLAttributes } from 'react';
import type { ImageMetaData } from '@sendbird/uikit-message-template';
export type Size = {
    width: number;
    height: number;
};
export interface ReactMessageTemplateImageProps extends ImgHTMLAttributes<HTMLImageElement> {
    tintColor?: string;
    metaData?: ImageMetaData;
}
export declare const RMTImage: ({ tintColor, metaData, ...props }: ReactMessageTemplateImageProps) => React.JSX.Element;
