import React from 'react';

import { createMessageTemplate } from '@sendbird/uikit-message-template';

import '../index.css';
import type { ReactParsedProperties } from '../styles';
import { parser } from './parser';
import { renderer } from './renderer';

export { parser } from './parser';
export { renderer } from './renderer';
export const { MessageTemplate } = createMessageTemplate<ReactParsedProperties>({
  renderer,
  parser,
  Container: ({ children, className }) => {
    return (
      <div
        className={`sb-message-template__parent ${className}`}
        style={{ display: 'flex', flexDirection: 'column', maxWidth: 400, marginBlockEnd: 24 }}
      >
        {children}
      </div>
    );
  },
});
