import React from 'react';

import type { ReactMessageTemplateImageProps } from './index';

export const RMTImageDefault = (props: ReactMessageTemplateImageProps) => {
  return <img {...props} onError={props.onError} draggable={false} referrerPolicy="no-referrer" />;
};
